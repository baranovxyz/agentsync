---
description: Authenticate user with OAuth
argument-hint: <provider> [scopes]
---

# Auth Command

Authenticate using the specified provider with optional scopes.

## Usage

Use this command to initiate OAuth authentication flow.

## Examples

```bash
/auth github read:user
/auth google profile email
```
